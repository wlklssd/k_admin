// Code generated by kadmin codegen. DO NOT EDIT.
import { request } from '../client';

export interface Product {
  id: number;
  name: string;
  sku: string;
  price: number;
  stock: number;
  active: boolean;
  description: string;
  publishedAt: string;
  createdAt: string;
  updatedAt: string;
}

export interface ProductPayload {
  name: string;
  sku: string;
  price: number;
  stock: number;
  active: boolean;
  description: string;
  publishedAt: string;
}

export interface ProductFilters {
  page?: number;
  pageSize?: number;
  keyword?: string;
  name?: string;
  sku?: string;
  description?: string;
  publishedAt?: string;
}

export interface PageResult<T> {
  items: T[];
  page: number;
  pageSize: number;
  total: number;
}

function queryString<T extends object>(filters: T) {
  const params = new URLSearchParams();
  Object.entries(filters).forEach(([key, value]) => {
    if (value !== undefined && value !== '') {
      params.set(key, String(value));
    }
  });
  const query = params.toString();
  return query ? `?${query}` : '';
}

export function getProductList(filters: ProductFilters = {}) {
  return request<PageResult<Product>>(`/api/product${queryString(filters)}`);
}

export function getProduct(id: number) {
  return request<Product>(`/api/product/${id}`);
}

export function createProduct(payload: ProductPayload) {
  return request<Product>(`/api/product`, {
    method: 'POST',
    body: JSON.stringify(payload),
  });
}

export function updateProduct(id: number, payload: ProductPayload) {
  return request<Product>(`/api/product/${id}`, {
    method: 'PUT',
    body: JSON.stringify(payload),
  });
}

export function deleteProduct(id: number) {
  return request<boolean>(`/api/product/${id}`, { method: 'DELETE' });
}
