// Code generated by kadmin codegen. DO NOT EDIT.
import { request } from '../client';

export interface {{.Class}} {
{{- range .Columns}}
  {{.JSONName}}: {{.TSType}};
{{- end}}
}

export interface {{.Class}}Payload {
{{- range .WritableColumns}}
  {{.JSONName}}: {{.TSType}};
{{- end}}
}

export interface {{.Class}}Filters {
  page?: number;
  pageSize?: number;
{{- if .HasKeyword}}
  keyword?: string;
{{- end}}
{{- range .QueryColumns}}
  {{.JSONName}}?: string;
{{- end}}
}

export interface PageResult<T> {
  items: T[];
  page: number;
  pageSize: number;
  total: number;
}

function queryString<T extends object>(filters: T) {
  const params = new URLSearchParams();
  Object.entries(filters).forEach(([key, value]) => {
    if (value !== undefined && value !== '') {
      params.set(key, String(value));
    }
  });
  const query = params.toString();
  return query ? `?${query}` : '';
}

export function get{{.Class}}List(filters: {{.Class}}Filters = {}) {
  return request<PageResult<{{.Class}}>>(`/api/{{.Prefix}}${queryString(filters)}`);
}

export function get{{.Class}}(id: number) {
  return request<{{.Class}}>(`/api/{{.Prefix}}/${id}`);
}

export function create{{.Class}}(payload: {{.Class}}Payload) {
  return request<{{.Class}}>(`/api/{{.Prefix}}`, {
    method: 'POST',
    body: JSON.stringify(payload),
  });
}

export function update{{.Class}}(id: number, payload: {{.Class}}Payload) {
  return request<{{.Class}}>(`/api/{{.Prefix}}/${id}`, {
    method: 'PUT',
    body: JSON.stringify(payload),
  });
}

export function delete{{.Class}}(id: number) {
  return request<boolean>(`/api/{{.Prefix}}/${id}`, { method: 'DELETE' });
}
